# docker-compose.gpu.yml
# Usage: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up
version: "3.9"
services:
  detector:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]